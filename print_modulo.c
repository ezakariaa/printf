#include "main.h"

/**
 * print_modulo - Prints the modulo (%) of a number
 * @arg: A pointer to the first arg
 *
 * Return: always 1.
 */
int print_modulo(va_list arg)
{
	(void)arg;
	_putchar('%');

	return (1);
}
